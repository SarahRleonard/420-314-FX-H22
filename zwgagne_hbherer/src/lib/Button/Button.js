import React from 'react'

const Button = () => {
    return (
        <div>
         <input className="favorite styled"
       type="button"
       value="Add to favorites"/>
        </div>
    )
}

export default Button
