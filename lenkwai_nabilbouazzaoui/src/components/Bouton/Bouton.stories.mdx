import { Meta, Props, Canvas, Story } from '@storybook/addon-docs';
import {Bouton, BoutonPrimary} from './Bouton';


<Meta title="Example/Bouton" component={Bouton} />

# Example Component Demo

## Bouton props
<Props of={Bouton} />
<Props of={BoutonPrimary} />

## Description

`Bouton` sert principalement de point d'entrée à une action entre l'utilisateur et le serveur.

## Demo

<Canvas>
  <Story name="Default Bouton">
    {(args) => <Bouton {...args} />}
  </Story>
   <Story name="Primary Bouton">
    {(args) => <BoutonPrimary {...args} />}
  </Story>
</Canvas>
